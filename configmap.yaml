apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: java-app
data:
  # Example key-value pairs (adjust for your app)
  app.version: "1.0"
  app.message: "Hello from ConfigMap"
  # Add more as needed, e.g.:
  # database.url: "jdbc:mysql://db:3306/myapp"

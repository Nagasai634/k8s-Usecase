apiVersion: v1
kind: Service
metadata:
  name: java-gradle-service
  namespace: java-app
  labels:
    app: java-gradle-app
spec:
  selector:
    app: java-gradle-app
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  type: LoadBalancer  # Changed from ClusterIP
